# APK FoodScan

Déposez ici le fichier APK de l’application (par exemple `app-release.apk`) pour permettre un test rapide sur Android sans compilation.

Pour générer l’APK :

```bash
flutter build apk --release
```

Puis copiez `build/app/outputs/flutter-apk/app-release.apk` dans ce dossier.
